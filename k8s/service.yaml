apiVersion: v1
kind: Service
metadata:
  name: grafanadb-restore-api
  namespace: db-operations
spec:
  selector:
    app: grafanadb-restore-api
  ports:
    - name: http
      port: 8000
      targetPort: http
